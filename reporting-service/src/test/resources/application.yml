spring:
  datasource:
    url: jdbc:h2:mem:reporting_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true

reporting:
  kafka:
    enabled: false
