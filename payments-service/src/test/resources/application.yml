spring:
  datasource:
    url: jdbc:h2:mem:payments_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password:
  flyway:
    enabled: true

payments:
  kafka:
    enabled: false
  outbox:
    relay:
      enabled: false
  processor:
    enabled: false
